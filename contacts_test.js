const addContact = require('./addContact');
const addContacts = require('./addContacts');

// console.assert(typeof addContact === 'function', 'addContact is not a function');
console.assert(typeof addContacts === 'function', 'addContacts is not a function');

